[package]
name = "tests"
version = "0.1.0"
edition = "2021"

[dependencies]
fadroma = { version = "0.8.7", features = ["scrt", "ensemble"] }
factory = { path = "../factory" }
auction = { path = "../auction" }
shared = { path = "../shared" }
